
declare interface Platform {
    OS: string;
}

export interface EventSubscription {
    remove(): void;
}

export interface EventEmitter {
    addListener<T>(type: string, onReceived: (data: T) => void): EventSubscription;
} 

export var Platform: Platform;
export var NativeAppEventEmitter: EventEmitter;
export var NativeModules: any;
